# Import thesis_data.sql and local_ip_addr.sql to your machine's database
# Replace <username> and <database_name> with appropriate values
mysql -u <username> -p <database_name> < thesis_data.sql
mysql -u <username> -p <database_name> < local_ip_addr.sql

